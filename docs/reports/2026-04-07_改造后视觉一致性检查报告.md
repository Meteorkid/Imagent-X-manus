# 改造后视觉一致性检查报告

## 范围
- 扫尾目录：`apps/frontend/app/(main)/admin/*`、`apps/frontend/app/(main)/settings/*`、`apps/frontend/app/(main)/templates/*`。
- 检查维度：颜色语义化、容器一致性、高度适配、重复实现清理。

## 本次完成项
1. 颜色语义化批量替换
- 将 `text-gray-*`、`bg-gray-*`、`border-gray-*` 批量替换为语义 token（如 `text-foreground`、`text-muted-foreground`、`bg-muted/30`、`border-border`）。
- 将 `text-blue-*`、`bg-blue-*`、`border-blue-*` 批量替换为语义 token（如 `text-primary`、`bg-primary/10`、`border-primary`）。

2. 结构一致性微调
- `AdminSidebar` 激活态边框统一为 `border-primary`。
- `AdminSidebar` 容器高度由固定 `100vh` 改为 `min-h-svh` 体系，适配移动端动态视口。
- 管理后台 logo 色块改为 `bg-primary`。

3. 重复实现收敛（延续项）
- 清理未引用的旧实现：`apps/frontend/components/tool/install-tool-dialog-new.tsx`。

## 抽样核验结果
- `admin`、`settings`、`templates` 目录已完成目标规则内的 `gray/blue` 直写清理。
- 仍保留的彩色语义（如 `red`、`green`）用于状态区分，未做强制替换。

## 质量检查
- 对本轮变更文件执行 lints：未发现新增 linter 错误。

## 残余风险与建议
- 风险：少量状态颜色仍使用原子色（红/绿）并非语义 token，后续可引入统一状态 token（success/warning/error/info）。
- 建议：
  - 增加视觉回归快照（手机/平板/桌面）；
  - 新增样式规范检查（禁止新增 `gray/blue` 直写）；
  - 在设计 token 层补充状态色语义映射。
